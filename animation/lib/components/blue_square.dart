import 'package:flutter/material.dart';

class BlueSquare extends StatelessWidget {
  const BlueSquare() : super();

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      height: 256.0,
      width: 256.0,
    );
  }
}
