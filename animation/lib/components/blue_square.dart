import 'package:flutter/material.dart';

class BlueLine extends StatelessWidget {
  const BlueLine() : super();

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      height: 256.0,
      width: 10.0,
    );
  }
}
